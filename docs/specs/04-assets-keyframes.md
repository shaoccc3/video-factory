# 04 素材庫與關鍵幀（P6）

狀態：已實作　日期：2026-09-23

- 上傳：kind ∈ logo／product／image／bgm／font；按文件頭判斷類型（不信任文件名與 Content-Type），分類型大小上限，文件名重新生成（`kind/年/月/uuid.ext`），展示名去除路徑與非法字元。
- 圖片與影片生成縮圖（ffmpeg，寬 360）；音頻記錄時長；標籤可篩選；刪除為軟刪除（已引用的任務可追溯）。
- 需要首幀的分鏡調用 Seedream，商品圖作參考圖；quick 類型可直接用素材庫圖片作首幀。
- 給模型的圖片地址：對象存儲預簽名 URL，有效期 `PRESIGN_EXPIRES_S`（預設 6 小時，覆蓋排隊時間）；`S3_PUBLIC_ENDPOINT_URL` 指向公網地址。本地存儲與雲端開發環境拉不到，圖生影片真實聯調放在有公網對象存儲的公司環境。
- 分鏡頁可替換首幀（素材庫選圖）或「重做關鍵幀」。
